import { n as parseChannelMessage, t as channelMessageJsonSchema } from "../tool-schema-CBjGPrsQ.js";
import { toolDefinition } from "@tanstack/ai";
//#region src/channels/tanstack-ai.ts
/** Adapt one Host-resolved surface to a TanStack AI server tool. */
function createSendMessageTool(host, surface, options) {
	return toolDefinition({
		name: options.name,
		description: options.description ?? "Deliver a message through this Channel",
		inputSchema: channelMessageJsonSchema,
		...options.needsApproval && { needsApproval: true },
		metadata: options.metadata
	}).server(async (value) => host.deliver(surface, parseChannelMessage(value)));
}
//#endregion
export { createSendMessageTool };

//# sourceMappingURL=tanstack-ai.js.map